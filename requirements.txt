fastapi
uvicorn
httpx
pillow
python-multipart
jinja2
python-jose
passlib
google-generativeai
numpy
requests

#http://localhost:8000
#uvicorn main:app --reload --host 0.0.0.0 --port 8000
#Step 1: Connect to Your VPS
#Option A: Using SSH (if your VPS runs Linux)
